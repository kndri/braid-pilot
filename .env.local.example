# Convex Configuration
# Get this from https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://YOUR_PROJECT.convex.cloud
CONVEX_DEPLOY_KEY=YOUR_DEPLOY_KEY

# Note: JWT_PRIVATE_KEY should be set in Convex environment variables, not in .env.local
# Generate and set JWT_PRIVATE_KEY:
# 1. Generate key: openssl genpkey -algorithm RSA -out private_key.pem -pkeyopt rsa_keygen_bits:2048
# 2. Encode key: cat private_key.pem | base64 | tr -d '\n'
# 3. Set in Convex: npx convex env set JWT_PRIVATE_KEY "<base64-encoded-key>"
# 4. Clean up: rm private_key.pem